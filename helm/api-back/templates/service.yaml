apiVersion: v1
kind: Service
metadata:
  name: api-back-service
  namespace: default
spec:
  type: { ? { .Values.service.type } }
  selector:
    app: api-back-nj-portfolio
  ports:
  - port: { ? { .Values.service.port } }
    targetPort: 8080
    protocol: TCP
